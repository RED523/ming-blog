# 页脚累计访问（Upstash Redis；Vercel Marketplace 绑定 Redis 后会注入下列变量）
# 注意：项目为 `output: 'export'` 时仍会生成动态 Route `/api/visits`，需部署到支持该接口的环境（如 Vercel Node）；仅将 `out/` 静态上传到 CDN 时，计数接口不可用。
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
