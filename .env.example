# 页脚累计访问（Upstash Redis；可在 Vercel 项目环境变量中配置，或通过 Marketplace 绑定 Redis）
# 部署：Vercel 上构建会自动关闭纯静态 export，以部署 /api/visits；本地 pnpm build 仍为静态 export（生成 out）。仅上传 out 到 CDN 时无 API，计数显示「—」。
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
